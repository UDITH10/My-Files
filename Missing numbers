import array as arr 
n = int(input("Enter the number of elements in array: "))
arry = []
for i in range(0, n):
    arry += arr.array('i', [int(input())])
n = len(arry) 
total = (n + 1)*(n + 2)/2
sum_of_Arr = sum(arry) 
print("\n")
print("Missing element is ", total - sum_of_Arr)
