i = int(input("Enter the start number: "))
j = int(input("Enter the end number: "))
for k in range(i, j):
    t = False
    num = k
    if num > 1: 
       for n in range(2, num): 
           if (num % n) == 0: 
               break
       else: 
           print(num)
